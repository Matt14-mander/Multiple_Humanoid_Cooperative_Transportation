# Airbot mesh placeholder

Place the matching `base_link.STL` and `link1.STL` through `link6.STL` files
from the same Airbot URDF package in this directory. The current downloaded
URDF references those files but the download folder contained the URDF only.
